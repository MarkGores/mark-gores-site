---
interface Props {
  id: string;
  title?: string;
}

const { id, title } = Astro.props;
---

<section id={id} class="section">
  {title && <h2 class="section-title">{title}</h2>}
  <slot />
</section>
